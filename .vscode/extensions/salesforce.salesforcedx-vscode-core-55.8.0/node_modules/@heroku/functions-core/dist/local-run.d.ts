import LangRunner, { LangRunnerOpts } from './lang-runner';
import { LocalRunProcess } from './local-run-process';
export declare class LocalRun {
    readonly lang: string;
    private readonly runners;
    constructor(lang?: string, runnerOpts?: LangRunnerOpts, runners?: Map<string, typeof LangRunner>);
    exec(): Promise<LocalRunProcess>;
    private autoDetect;
}

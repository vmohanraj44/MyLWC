import { Connection } from '@salesforce/core';
/**
 * TraceFlagsRemover is a singleton which deletes trace flags created by the extensions.
 */
export declare class TraceFlagsRemover {
    private static _instance;
    private connection;
    private newTraceFlagIds;
    private constructor();
    static getInstance(connection: Connection): TraceFlagsRemover;
    static resetInstance(): void;
    addNewTraceFlagId(newTraceFlagId: string): void;
    removeNewTraceFlags(): Promise<void>;
}
//# sourceMappingURL=traceFlagsRemover.d.ts.map